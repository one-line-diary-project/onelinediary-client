import { Fragment } from "react";
import NotFoundError from "../components/NotFoundError";

const NotFound = () => {
  return (
    <Fragment>
      <NotFoundError />
    </Fragment>
  );
};

export default NotFound;
